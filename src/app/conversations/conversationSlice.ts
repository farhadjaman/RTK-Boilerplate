import { createSlice } from '@reduxjs/toolkit';
const initialState = {};

const conversation = createSlice({
  name: 'auth',
  initialState,
  reducers: {},
});

export const {} = conversation.actions;
export default conversation.reducer;
